{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "lerna": "^2.5.1",
    "rimraf": "^2.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
